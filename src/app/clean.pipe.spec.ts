/* tslint:disable:no-unused-variable */

import { TestBed, async } from '@angular/core/testing';
import { CleanPipe } from './clean.pipe';

describe('CleanPipe', () => {
  it('create an instance', () => {
    let pipe = new CleanPipe();
    expect(pipe).toBeTruthy();
  });
});
